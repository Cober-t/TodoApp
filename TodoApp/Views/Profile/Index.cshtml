@model IEnumerable<Project>

<h1 class="fw-bold align-items">PERFIL</h1>

<div class="align-items-md-stretch">

    @* <button type="submit" action="AddEditProject">Add Project</button>

    <div class="row align-items-md-stretch gap-4">
        @foreach (Project project in Model)
        {
            <div class="col-md-6">
                <h1>@project.Title</h1>
            </div>

            <button action="AddEditProject" type="submit" class="fw-bold fs-5 text-blue">...</button>
        }
    </div>  *@
</div>